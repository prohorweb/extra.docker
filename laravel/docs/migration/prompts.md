"# AI Prompts Library\n\n---\n\n## 1. Generate Feature Module (e.g., \"Services\")\n\n**Prompt**:\n\n```\nCreate a feature module \"Services\" with the following components:\n\n- **Data Layer**:\n  - `ServiceCardData`, `ServiceDetailData`, `ServiceFilterData` (DTOs)\n  - `ServiceService` (aggregates data from models)\n\n- **Alpine.js**:\n  - Blade components: `x-features.services.card`, `grid`, `detail`\n  - Alpine stores for filtering/pagination\n\n- **Pages**:\n  - `pages.services.index.blade.php`\n  - `pages.services.show.blade.php`\n\n- **SEO**:\n  - `SEOData` for index/show pages\n  - JSON-LD for `Service` type\n\n- **Admin Panel**:\n  - `ServiceResource` in Filament (CRUD, filters, export)\n\n- **Filters**:\n  - Support filtering by category/tag\n  - `ServiceFilterRequest` validation\n\n- **Tests**:\n  - Feature test for index/show pages\n  - Unit tests for DTO conversions\n\n**Constraints**:\n- Use Design Tokens (`--color-brand`, `--radius-card`) for styling\n- Blade components must follow `x-features.{domain}.{name}` naming\n- No direct Eloquent in Blade\n- Mobile-responsive grid with Tailwind variants\n```\n\n---\n\n## 2. Migrate CSS Component (e.g., \"Trainer-Card\")\n\n**Prompt**:\n\n```\nMigrate the existing `trainer-card` component from `styles.css` to Tailwind v4.\n\n- Use ONLY design tokens:\n  - `--color-brand` for primary colors\n  - `--radius-card` for card radius\n  - `--shadow-card` for hover effects\n\n- Create `x-features.trainers.card` component\n- Avoid `@apply` chains longer than 3 tokens\n- Ensure responsiveness (mobile/desktop variants)\n- Reuse existing media URLs from models\n\n**Example structure**:\n```blade\n<x-features.trainers.card :interactive=\"$active\" :service=\"$trainer\">\n    <x-slot name=\"image\"><img src=\"{{ $trainer->imageUrl }}\" alt=\"Trainer\"></x-slot>\n    <x-slot name=\"content\">\n        <h3 class=\"text-lg font-heading\">{{ $trainer->name }}</h3>\n        <p class=\"text-sm\">{{ $trainer->specialization }}</p>\n    </x-slot>\n    <x-slot name=\"footer\">\n        <x-ui.button size=\"sm\" href=\"{{ route('trainers.show', $trainer->slug) }}\">Profile</x-ui.button>\n    </x-slot>\n```\n\n---\n\n## 3. Create Login/Registration Forms + Filament Integration\n\n**Prompt**:\n\n```\nImplement Login + Signup forms with:\n\n- **Form Requests**:\n  - `LoginRequest`, `SignupRequest` validation\n  - `PasswordResetRequest` flow\n- **Alpine.js integration**:\n  - `x-ui.form-handler` for form submission\n  - Real-time feedback\n- **Email Verification**:\n  - Confirmation email with Magic Link\n- **Password Reset**:\n  - Token generation + email link\n- **Filament Admin**:\n  - `UserResource` with roles/permissions\n  - Dashboard showing users with activity logs\n- **Tests**:\n  - Feature tests for authentication flow\n  - Unit tests for email sending\n\n**Requirements**:\n- Use `laravel/breeze` as base but customize with Design System\n- No third-party auth libraries\n- Form H oneypot field\n```\n\n---\n\n## 4. Add Cookie Consent Banner\n\n**Prompt**:\n\n```\nCreate `x-features.forms.cookie-consent` Alpine component that:\n\n- Implements `cookieconsent.min.js`\n- Options:\n  - `message`: 'We use cookies for analytics and personalization.'\n  - `button`: 'Accept'\n  - `strict`: true (blocks non-consenting traffic)\n- Integrates with Laravel Sessions\n- Displays only on first visit\n- Uses Design System colors (`--color-brand` for button)\n\n**Implementation Steps**:\n1. Add script in `resources/views/layouts/app.blade.php`\n2. Create component with cookieconsent configuration\n3. Add consent flag to `Alpine.store('consent', { cookie: false })`\n4. Redirect to `/consent` if disabled\n```\n\n---\n\n## Navigation\n\n- [← Roadmap](./roadmap.md)\n- [Foundation →](./foundation.md)\n- [Patterns →](./patterns.md)\n- [Docs →)](./docs.md)"}