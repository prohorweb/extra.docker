"# Troubleshooting Guide\n\n---\n\n## Common Issues\n\n### Tailwind CSS Issues\n- **Problem**: Tailwind classes not rendering\n  - **Solution**: Verify `@theme` usage in `app.css`, run `npm run dev`\n  - **Check**: Ensure `content` array in `vite.config.js` includes `resources/css/app.css`\n\n- **Problem**: Unknown utility classes\n  - **Solution**: Run `tailwind generate classes` or check `@theme` definition\n\n### Alpine.js Issues\n- **Problem**: Component not reactive\n  - **Solution**: Ensure data bindings use `alpine.store` properly\n  - **Check**: Look for Vue-style syntax in components\n\n- **Problem**: Forms not submitting\n  - **Solution**: Verify `form-handler` component integration\n  - **Check**: Ensure CSRF token is in `<meta name=\"csrf-token\">`\n\n### Filament Issues\n- **Problem**: Resources not appearing\n  - **Solution`: Re-run `php artisan filament:serve`\n  - **Check`: Verify resource `getNavigationGroup()` implementation\n\n- **Problem`: Permissions issues\n  - **Solution`: Review `App/Filament/AdminPermissions.php`\n\n### Docker Issues\n- **Problem`: HMR hot reloading broken\n  - **Solution`: Update `vite.config.js` with correct host/port:\n  ```js\nhmr: {\n      host: 'extra.new',\n      protocol: 'wss',\n      clientPort: 443\n  }`\n- Check browser console for CORS errors\n\n### SEO & Performance\n- **Problem`: Missing OG tags\n  - **Solution`: Add `x-seo.meta` component with OpenGraph data\n- **Problem`: Slow Lighthouse score\n  - **Solution`: Optimize images via Media Library, enable browser caching\n\n---\n\n## Quick Fixes\n\n### Rebuild Tailwind Config\n```bash\nnpm run build\n# or `tailwindcss -i resources/css/app.css -o public/css/app.css`\n\n### Clear Cache\n```php\n// In kernel.php\nCache::forget('route.{{ request()->route()->uri() }}');\n\n### Component Styling\n- Use `purge` in Victor \u2018icon-pace\u2019 mode for new classes\n- Run `npm install` after adding new utilities\n\n---\n\n## Navigation\n\n- [← Roadmap](./roadmap.md)\n- [Foundation →](./foundation.md)\n- [Patterns →](./patterns.md)"}