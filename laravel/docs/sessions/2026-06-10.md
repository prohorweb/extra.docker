# 2026-06-10

## What was done

### Commit e93e78d: Separate Yii2 and Laravel for different hosts (extra.new and extra.loc)
- Created `nginx copy.conf` with full configuration for both hosts
- Updated `nginx.conf` to serve:
  - `extra.loc` → Yii2 frontend (`/frontend/web`)
  - `extra.new` → Laravel (`/laravel/public`)
- Adjusted CSS files in Laravel (`app.css`, `global.css`) for the new setup

### Commit 55b3ffa: Rewrite Docs and add Fonts
- Restructured documentation: consolidated two `ai/` folders into `laravel/docs/ai/`
- Added comprehensive sessions structure with daily entries
- Added Oswald and Roboto fonts (variable + static weights) to Laravel
- Removed FontAwesome CSS from css/ folder (moved to fonts/)
- Cleaned and standardized migration documentation in English
- Created domain files: `home.md`, `map.md`

## Problems and solutions

**Problem**: Need to run Yii2 and Laravel simultaneously on different hosts
**Solution**: Configured Nginx with two separate server blocks, each pointing to its own document root

**Problem**: Documentation was scattered across two `ai/` folders
**Solution**: Consolidated everything under `laravel/docs/ai/` with standardized structure

## Status

- ✅ Nginx configuration for dual-host setup complete
- ✅ Documentation restructure complete
- ✅ Fonts added to Laravel (Oswald, Roboto)
- 🔄 Homepage migration analysis started (HomeController created, route defined)

## Next steps

1. Continue homepage migration - build Blade components (navigation, hero, actions, subscribe)
2. Connect data from Eloquent models (Club, ClubBanner, Share, Metro)
3. Test Nginx dual-host configuration
4. Update CURRENT_STATE.md to reflect progress